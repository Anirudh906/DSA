# Write your MySQL query statement below
SELECT DISTINCT U.NAME, IFNULL(SUM(DISTANCE) OVER (PARTITION BY USER_ID), 0) AS TRAVELLED_DISTANCE FROM RIDES AS R RIGHT JOIN USERS AS U ON U.ID = R.USER_ID ORDER BY TRAVELLED_DISTANCE DESC, NAME ASC;